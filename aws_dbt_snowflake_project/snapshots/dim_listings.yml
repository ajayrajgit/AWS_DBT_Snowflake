snapshots:
    - name: dim_listings
      relation: ref('listings')
      config:
        schema: gold
        database: AIRBNB_RAW
        unique_key: listing_ids
        strategy: timestamp
        updated_at: listing_created_at
        dbt_valid_to_current: "to_date('9999-12-31')"